// Scanning a recipient address via the device camera is mobile-only.
// On web there is no camera scan flow, so this renders nothing.
const AddressScanButton = () => null

export default AddressScanButton
