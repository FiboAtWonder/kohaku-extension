# Account recovery UX — status & handoff

> **Purpose:** the single catch-up document for this workstream. Any agent or teammate resuming the work reads this first. **Rule: update this file at every checkpoint** (wireframe commit, decisions batch, artifact republish, review round). Last update: 2026-08-27 — **fifth review round applied (0xtiti, 32 threads).** Two Opus fronts verified every comment against the docs, the wireframe JSON and the contracts spec; Fibo ruled all 36 findings; applied as **decisions 99–102**: **99** privacy levels renamed to **Private (now the DEFAULT — matches their I-16 "private by default") / Shape visible / Public**, existence is NEVER hidden so copy says "nobody can READ your setup", SDK **enum literals unchanged** (display names only, Fibo's call) · **100** — **24h wallet-side minimum** waiting period, Instant (0h) removed from the picker, 48h default, chain still floorless (amends 85) · **101** — the REC-29 "cancel may not land" note REMOVED (spec copy + the drawn D2-01 second banner state rolls back) · **102** — 1-1 paths stay supported (no preset is 1-1; demo keeps Sam's single-method journey). Also applied: E2 section deleted (89 keeps the history), Flow F reduced to a 3-line stub, wizard mermaid renumbered to decision 58's ten steps, fast-track label → "N OF 4", five paste errors (added "already-added" + "expired"), Milestones → **MVP ~81 / V1 ~43**, "## Decisions log" heading, card contents = address+password+how-to+"cannot move funds" (URL dropped), IPFS framing fixed, "Removing…" verb fix, I-10 misread fixed in 4 spots ("one attempt AT A TIME", not "ever"), ~18 superseded-wording passages swept, dated-ruling parentheticals stripped (35 spots, log tables keep theirs), both doc version histories moved to this file (bottom). SDK doc → **v10** (getRecoveryConfig marked OPEN with a 4-state candidate; prepareSetup takes ONE EncryptedPath; prepareUpdate(account,next); Claim.attemptId + voidedClaimIds + expectedAttemptId; getRecoveryAttempt with status+cancelCause replaces the pending-only read; pack/unpackBackupSet pure; Colibri item moved out; createClaim glossed, NOT renamed) · extension doc → **v6** (Colibri = new MVP item 9, session keeps predicted attemptId, backup-set ownership) · kit-asks → **v3** (**REC-42** does the cancel event name the cause? · **REC-43** make the validity window long (weeks) · REC-24 demoted — with Private default a negative answer no longer collapses the default). All 32 PR threads replied with the commit reference. **Pen round drafted** (12-screen sweep: 0h/floor strings on 11 screens, the Private rename on C-06e/D-04/D-06/D-06f, C-08/C-08b card fix, D-08's three fixes incl. the "no time limit" contradiction, the graded meter still on G-02/G-03b/G-04/D2-03, C-06e "Optional."/"never stops a recovery" leftovers, D2-01 REC-29 rollback) — awaiting Fibo's run + ⌘S; **demo rebuild v12 queued after it** (the C-06e rename moves Alice/Carl/Diana's hotspot target and stories). Earlier — Last update: 2026-08-25 (second round) — **final reconciliation calls landed: decisions 97–98, SDK v9, kit-asks v2.** Fibo's four remaining calls: REC-1 → **assume all three privacy levels exist, carried on the contract's event logs** (decision 97 + SDK assumption A7; kit-ask REC-24 confirms the missing 'Hide the details' shape — structure clear, values encrypted in the setup event's backup field; default stays Hide-the-details; if the kit says no, 97 reopens) · REC-18 → the disclosure-rendering guarantee is the UX's, not the SDK's (kit-asks §3 ready) · REC-19 → the `slot` question is a confirmed kit-ask · REC-35 → disclosures **#4/#5/#6 = MVP copy, #1/#2/#3/#7 = V1** with grading (decision 98; copy placed on C-05/C-06e/C-07). REC-25 resolved: the **D-04/D-06 readout is two-state** (masked-values vs locked 'enter the recovery password from your card'), drawn into the chapter-1 mermaid. The reconciliation report is down to **7 rows**: REC-15 (Fibo sends the docs himself), 16/19/24 (ready in kit-asks, ride with 15), 20 (recorded), 32 (blocked on their Q-4 width), 41 (kept open). **Wireframes v15 LANDED (reconciliation round, decisions 90–98) — verified CLEAN by an Opus 5 subagent** (git-JSON vs v14.2, keyed by (band, name) — NOTE for future verifiers: 25 codes are duplicated in the OUTDATED band, a name-only key silently drops them): exactly 22 screens edited (C-05c/d, C-06, C-06e Confirm+reveal lines+required, C-07/C-07b trust block, C-08/C-08b card password row, D-02/D-02b discovery help, D-06 masked values, D-06f→locked state, D-07 expiry chips, D-11/D-11b what-this-removes, D-15/D-15c re-setup+re-salt+softened claim, D2-01 three cancel notes, E1-02/E1-03 valid-until+destination key, G-02/G-05b cancel warnings) + 4 added (D-04e already-running, D-07m approvals-void, D-07n approval-expired, D-01b migrate-assets w/ V1 caption chip); 165 wrappers = 137 active + 28 OUTDATED; zero renames, OUTDATED/X/A/B/F byte-identical, off-limits zkPassport screens untouched; clipping baseline 1+4 confirmed by the Pen agent. Pen agent auto-corrected two prompt code mismatches: Recovery Card lives on C-08/C-08b (not C-07e), account entry on D-02/D-02b (not D-04). **Demo REPUBLISHED (label reconciliation-v15, 83 screens, journeys v11):** the Pen restart revived the bridge; 15 hotspots re-measured live (C-06e's retargeted onto the 'Hide the details' option row — the old string is gone; C-08/D-07 rects unchanged), 4 sizes corrected, 8 step stories updated (incl. a Sam bridge note: his Public-level card prints NO password, decision 90), the 4 new screens joined the Extras card (6→10 steps, flows C/D/X), full 83-frame re-export (exports not committed — regenerable from the .pen), publish needed force:true again (tracker held the other session's gas-deposit-87 version), favicon set 🛟 (keep it stable). The spent REBUILD-v11-plan.md was deleted. Earlier: demo rebuild v11 RULED (Fibo: demo now, deck stays out) but was briefly **BLOCKED on a dead Pencil MCP bridge** (the Pen app restarted after the session's bridge connected) — the full one-pass plan (4 stale sizes, 15 hotspot rects to re-measure, 4 new screens → Extras, 83-frame export list, artifact URL + label `reconciliation-v15`) is committed at `social-recovery-ux-assets/persona-demo/REBUILD-v11-plan.md`; fix = reconnect the `pencil` MCP (session restart or /mcp), then execute the plan. Original draft note: 16 edits + 4 new screens (D-04e already-running, D-07m approvals-void, D-07n approval-expired, D-01b migrate-assets); the zkPassport remap round (52 screens + renames + persona remap + X-02/deck strings) stays QUEUED separately. Old queue note:  the D-04/D-06 locked state, C-06e per-level reveal lines, C-05 identity≠presence line, C-07 disclosure block. Notion replicas RETIRED as review surfaces (Fibo, 2026-08-25): reviews happen on the GitHub PR; do not sync Notion any more (the replica pages stay frozen at spec pre-90 / SDK v7). Earlier the same day — **reconciliation RULINGS APPLIED.** Fibo ruled on all 41 REC items; the approved fixes landed as **spec decisions 90–96** (90 card carries the password + required-at-hidden-levels · 91 approvals expire, supersedes 9, amends 70 · 92 linkability conditional on per-account salts · 93 instance uniqueness = wallet policy · 94 our presets over their tiering, all four methods stay · 95 C-06e Password + Confirm, no MVP recall test · 96 reconciliation states batch with milestone tags), **SDK doc v8** (eleven-field digest over `keccak256(setup_body)`, `Handover` type, `PendingRecovery` drops `satisfiedMethodIds`, `PreparedTx` third-party-call kind, `Claim.expiresAt`, new §2.7 SDK duties incl. per-account salts, §2.6 rewritten to the card model, adapter = KIT-TEAM deliverable), **extension doc v5** (adapter from the kit; watcher = whole event-reader, kept open), and the new **`social-recovery-kit-asks.md` v1** (attempt-event fields ask REC-16, slot question, fourth-privacy-shape ask, settled positions: 48h default, decision 51, Q-12 copy, Q-14 follow-on). Applied by two Opus 5 agents, diff-verified (no hard wraps, mermaid balanced, safety header correct). The reconciliation report is slimmed to open items only: **awaiting Fibo — REC-1 level mapping (recommend asking for a structure-public/values-encrypted shape), REC-18/19 send-go, REC-35 disclosure milestones**; REC-15 Fibo sends the docs himself; REC-25/32 blocked; REC-41 open. **Pen round QUEUED, not run:** C-06e Confirm field + required/absent states, Recovery Card password line, Flow D already-running band + discovery help + approvals-void + per-claim expiry countdowns + D-11 destination-key/what-this-removes, D2 cancel-may-not-land note + three cancel routes, E1 valid_until + destination key + publication line, G-05/G-02 cancel warnings, C-07 trust-list/disclosure block, C-06 repayment row (V1). **Notion replicas are STALE** (spec at pre-90, SDK at v7) — sync only on request. Earlier the same day — **contracts spec-v1 RE-CHECK done** against their PR #3 head `5e252fbee` (nine commits past the review baseline: R-14…R-17 rounds + a review sweep) and our head `a40e7410e`, via two Opus 5 fronts; results in `social-recovery-contracts-spec-review.md` — since restructured (Fibo's ask) to an **open-items-only doc with stable REC-1…REC-41 ids** (rulings / doc fixes / outbound / undrawn UX); the full review history lives in git (`a40e7410e`, `688a89450`) and the appendix's raw tables. The big flip: **they dropped the timelock floor entirely** (new I-7: "the kit enforces no minimum"), so the decision-85-vs-I-7 conflict is GONE — Instant (0h) saves cleanly, the drawn 0h chip stands. Also resolved/obsolete: Q-3 deleted (decision 73 no longer needed as an unblock), E2/Q-9 closed by decision 89, A5's UserOp clause + the funding gap fixed by 87. **Inverted:** the wait moved INSIDE the commitment (I-16; the `SetupCommitted` event dropped its `wait` arg) — do NOT surface it as public, and no countdown screen has a chain source any more → new ask: the attempt-opened event must carry handover + wait + finalize deadline. **Still open on this branch (report §2, REC-5…14):** the four factual copy errors (multichain `:9`, Flow G `:408`, no-expiry `:322/:337/:466` — now invariant-backed by their new I-13, linkability `:159` — now conditional on per-account salts), decisions 56/48 + SDK §2.6 contradicting our own 84, the SDK shape drift (eleven-field digest over `keccak256(setup_body)`, four new SDK duties incl. per-account salt minting), the approval screen must render the destination key (their Q-8 homes it on us), no kit-shipped watcher any more, all §4 frictions except funding, terminology rename manifest→"trust-root declaration". **Nothing has been sent to the contracts team yet**; Fibo owes 4 rulings (report §1, REC-1…4: default privacy level + card contents, 48h-vs-days wait default, tiering/presets incl. their newly blessed passkey-only shape, decision-79 contingency if Aadhaar is cut). Invariant numbering shifted (+1 above I-12; new I-13 = proof expiry). Earlier the same day (late) — **two review amendments applied (`9c49bf8ae`), from Fibo's own PR comments:** **88 amended** — the recovery fast-track creates JUST an EOA that becomes the signer of the recovered account (which already is a smart account); smart-account-at-init stays a MUST for the STANDARD create flow only (spec Flow A1 bullet + log row 88, SDK A5, extension item 1). **84 amended** — choosing the Public visibility level sets NO recovery password at all, nothing is encrypted (spec visibility paragraph + log row 84 + wizard 5b node, SDK §2.6 + §3 — `encryptConfigValues` drops `'public'` from its type; `prepareSetup` keeps all three levels). SDK doc bumped to **v7**; Notion replica synced to v7 (also repaired v6-sync drift the silent-skip gotcha had left: the `prepareSetup` table row lacked the visibility param, §5 Q1/Q5 were stale). Both PR threads replied. **Queued for the next Pen round:** C-06e's password fields deactivate when the Public radio is picked. Also new this session: **`.gitattributes`** (`25a589ecd`) marks everything but `social-recovery-ux-flows.md` + `social-recovery-sdk-requirements.md` as `linguist-generated`, so the PR collapses those diffs. Earlier — **wireframes v14 LANDED (`4ed20c772`)**: the decisions 84–89 Pen round is drawn and verified CLEAN by an Opus 5 subagent (git-JSON vs `73ec9ff57`: exactly 2 screens added — **A1-04** "Gas deposit · fund your new account" and **D-09** "Gas deposit · before you submit" — and exactly 13 edited: A1-01..03 counters "OF 4", C-06/C-06c Instant (0h) chip + picker-only warning + 24h-minimum line removed, C-06e visibility radio (3 options, Hide-the-details selected), B-01 honesty reword + synced-variant annotation, D-01 two-accounts panel, D-12 "Top up gas and retry" + new-account copy, G-01/b/c meter → plain "Recovery is set up" status, X-02 refresh with no ZK Email/.eml strings; OUTDATED band byte-identical, zero renames; fresh wrapper count **161 = 133 active + 28 OUTDATED** — the old "158" figure was one low). **Decision-85 leftovers SWEPT in two micro rounds, both verified CLEAN:** **v14.1** (`3ed4905c4` — G-01/b/c "Minimum 24h" foot line deleted, C-07/b/g hint → "There is no enforced minimum — the default is 48 hours.", X-01 → "no enforced floor, 48h default", C-06 decorative chips row aligned with the picker) and **v14.2** (`0095aae3d` — C-10/C-10b/C-10c/C-10d/C-10f `Min line` → "No enforced minimum — default 48 hours."). Zero 24h-floor strings remain outside the frozen OUTDATED band. Deliberately NOT touched (no ruling asked/needed yet): D-12's OPEN QUESTION Q7/Q19 panel (decision 87 arguably closes it) and C-06c's selected chip (moved 72h→48h in v14 — flagged, Fibo has not asked for a revert). **Demo REPUBLISHED (label gas-deposit-87, 79 screens, journeys v10 `93fbf0865`):** A1-04 joined Sam/Alice/Carl/Diana after A1-03; D-09 joined Bob between D-07j and D-11 (logged-in entry = never saw A1-04, honest showcase; no hotspot — the screen auto-advances on deposit, matching the drawn Waiting strip); the other four got a D-11 "funded at A1-04, D-09 auto-skips" note; Sam's D-11 spec Q7/Q19 → decision 87; decision-84/85 story+spec fixes (Alice's C-06e = the Hide-the-details level, Diana's C-06/Bob's C-10 floor wording); 7 stale hotspots re-measured by an Opus 5 subagent from the .pen JSON (B-01 +26.5y, C-06 +46y, C-06c −33y, D-01 +79y, C-06e button moved to y=717, A1-01/A1-03 minor) — the measurement engine survives at the session scratchpad `measure/` dir. Full 79-screen re-export via Pencil MCP `Export` (name→live-id resolution; all 79 device-frame names are unique file-wide, OUTDATED wrappers use different child names). Republish needed `force:true` — the artifact tracker held another session's blank-start-82 version, already contained in the repo sources. The deck's stale pre-84 Milestones copy stays queued with the zkPassport round. Earlier the same day — **fourth PR review round applied (decisions 84–89)**, second reviewer 0xtiti joined. The big one: **Q29/53 is ANSWERED — commit-reveal** (84): encrypted logs + recovery password, three user-selectable hiding levels exposed on C-06e (Hide everything / Hide the details = default / Public); the MVP blocker falls. Also: **timelock floor removed** (85 — 0h legal, picker-only warning, 48h default) · **fragility grading + graded meter → V2** (86; plain status line before) · **funding: user-funded MVP with gas-deposit screens A1-04 + D-09, auto-skipped when funded; execute repays msg.sender (may change); paymaster sponsorship V1** (87) · **smart account at initialization is a MUST; existing-account recovery = key signs two accounts, copy required** (88) · **E2 async approvals skipped** (89) · ZK Email history stripped from flows/SDK prose (log only) · passkey honesty note reworded (device-bound vs synced) · D chapter-1 mermaid simplified, chapter-2 email node → zkPassport. Docs: spec decisions 1–89, SDK **v6**, extension **v4**. **Pen round (now landed as v14, see above)**: A1-04 + D-09 gas-deposit screens, C-06e visibility choice, C-06/C-06c 0h chip, B-01 honesty reword, D-01 two-accounts panel, G-01 plain status line, X-02 refresh. zkPassport screen redraws blocked on the TEAM DECISION over the research verdict (sixth round: research done, verdict negative for MVP — see `social-recovery-zkpassport-research.md`). Earlier — 2026-08-20 (sixth round) — **zkPassport research pass done**: findings in the new `social-recovery-zkpassport-research.md` (synthesis + three raw Opus 5 fronts as appendices; live sources and `eth_call` chain reads). **Verdict: do not ship zkPassport as an MVP recovery method on this evidence** — the enrolled identifier dies with the document (nullifier hashes the SOD signature; expiry asserted in-circuit), a proof does not prove presence and no document can be revoked, and the CSCA root has a single unprotected EOA writer. No external audit; circuits not frozen; ~455 MB app install mandatory; cloud prover needed for the EVM proof; **unverified whether the flow works from a `chrome-extension://` origin — prototype that first**. The pass closed 7 of 10 Pen gates (B1 = phone hand-off, so 2–4 NEW screens, not a redraw; B3 = C-05n is replaced not deleted, so ~76 MVP screens holds; B4 = the "local only, no chain" promise cannot hold for this method). Blast radius corrected from "4 screens" to **52 MAIN screens + 6 frame renames**, one single-shot Pen round. **Awaiting Fibo:** the option ruling (recommended: move zkPassport to V1 behind mitigations), plus gates B8/B9/B9b/B10 (preset name, method label, the value fixture replacing `f•••@gmail.com`, one ZK-ID card vs two rows). Earlier the same day (fifth round) — **decision 82: blank start + the MVP configuration envelope**. A fourth C-01 card "Start from scratch" opens the new empty editor **C-04f**; **C-10b + C-10e move into MVP** (editing and blank building need a member picker and an enroll sheet); multi-group is reachable in MVP through the editors, so #75's "Advanced-only" now means the free-canvas builder, not the capability; C-04e drops its "Open Advanced" button; a path loses its preset label once editing changes its shape (#69 amended). Counts: **MVP ~76 / V1 ~42**. Landed: spec (`def6b17e8`), **wireframes v14** (`2c29dd923`, 159 wrappers — verified PASS, only C-01 + C-04e changed and C-04f added), deck slide republished (`879bcf782`, label `decision-82`), demo republished (77 screens, label `blank-start-82`; C-04f added to Extras). Fixed in passing: C-01 grew 800→910 and Alice's "Guardians only" hotspot was 41px stale since the "Deliberately strict" note landed. **Notion replica REPAIRED and synced (2026-08-20).** A malformed `<td>` in an earlier sync had dropped the Answer column of the whole decisions log; Fibo declined a history restore, so the log was rebuilt row by row from the spec and verified by an Opus 5 subagent: 83 rows, every answer present, nothing truncated, all four inline-discussion anchors intact. The replica also gained rows **80–83** (it had stopped at 79) and finally took the MVP ~76 / V1 ~42 counts — the earlier attempt had failed silently because Notion escapes the tilde. Sync gotchas recorded in the `social-recovery-ux-doc-sync` memory: never anchor an op mid-tag, the tool skips non-matching ops silently while reporting success, and escaped characters break anchors. Earlier: 2026-08-20 (fifth round) — swept **four follow-up PR comments I had missed**. Two change the docs: **proving is the SDK's job behind one call** (SDK assumption A4b — it runs in the extension only because that is where the SDK is loaded; we choose the host context, not the implementation), and the **passkey health check is a user-gesture test by design** (decision 83 — we prompt, the user taps; background runs cover only the environment). Also added per request: an **interface summary** section to the SDK doc (§3, signatures only). SDK doc **v5**, and the **Notion replica is synced to v5** (interface summary + A4b/A6/A5 + decision 83). Earlier the same day (fourth round) — **decisions 81**: safety grading is OURS, not the SDK's (it serves many wallets, each with its own bar — so the SDK returns metadata, we own the rules, the meter and the honesty copy); and the 4337 account may reuse an **existing EOA as its signer**, so a current user keeps their key (new Flow C route, not drawn yet). Persona remap ruled: everything that used ZK Email maps to zkPassport (Diana, Bob) in the queued Pen round. Funding/paymaster ownership stays **unanswered** — both rails specified. SDK doc **v4**, extension doc **v3** (new item 2 = fragility grading + meter). Earlier the same day (third round) — **wireframes v13** (`16f21c650`): new **X-02 rollout-milestones team exhibit** (band `Flow · X`, after X-01), verified PASS by an Opus 5 subagent (excising X-02 reproduces the baseline document hash exactly). Demo republished with a new **"Rollout" card** (between Bob and Extras, one step = X-02; label `rollout-section`, 76 screens); X-01 stays in Extras. **Open ruling for Fibo:** X-02, the deck's Milestones slide and the spec's Milestones section still say ".eml help sheet" / "ZK Email relayer + prover" — stale under decision 80. Options: patch to neutral wording now, or fold into the queued zkPassport Pen round (recommended — the method name is not final). Earlier the same day (second round) — **decision 80: zkPassport replaces ZK Email** (Aadhaar stays), and the account substrate is a **bare-bones 4337 implementation**, not the Ambire stack — so paymaster sponsorship becomes the primary funding path and the Ambire-specific blockers die. SDK doc is at **v3** (stateless SDK: sessions are extension state; `evaluate` stays SDK as a pure function; guardian page is extension-built over two SDK calls), extension doc at **v2**. **Queued, not run:** the zkPassport research pass, then the Pen round to redraw C-05e/C-05k/C-05n/D-07e + the "your device + your ID" preset; wireframes and demo still show ZK Email until then. The SDK doc is now **also on Notion** ([SDK requirements — Account recovery](https://app.notion.com/p/3c29a4c092c7812b8b4bdbb6ae3001ec), child of the UX draft, linked from its header). Earlier the same day: **SDK requirements v2** after Fibo's 19 PR #2 review comments: assume-the-SDK-works posture, extension work split into the new `social-recovery-extension-work.md` (MVP/V1/V2 tagged), decision 77 amended in the spec (reply-based .eml flow, ruled in review), **ZK Email very probably replaced by zkPassport** (team decision pending — gates §2.3 of the SDK doc, the C-05e/D-07e/C-05n wireframes, the C-05n queued Pen items, and the relayer question; zkPassport research not yet run). Earlier: 2026-08-19 — **milestone split landed as decision 79**: MVP / V1 / V2 mapped in the spec's new "Milestones" section. Fibo's rulings: all 4 methods MVP · guardian page → V1 with a manual sign-this-payload MVP mechanic on the D-07 row · post-recovery cleanup + post-cancel triage → V2 (cancel itself MVP) · recovery password MVP · path editing MVP. Q29/53 is now MVP-blocking. Queued Pen items (not run): the D-07 guardian manual-sign state; honesty copy on the MVP done/cancelled terminals. Deck republished with a Milestones slide (label milestones-79). Earlier the same day: **SDK requirements v1 drafted** (`social-recovery-sdk-requirements.md`): three-front Opus 5 investigation (contracts+kit · extension · proof methods incl. prover benchmarks) merged into a 6-section requirements doc. Pending Fibo: ruling on finding **R1** (decision 77 must become reply-based — zk-email proves the email the user SENDS; spec/wireframes C-05e/D-07e/C-05n need a small amendment) and R2 (Aadhaar upstream broken/unmaintained — tag at risk). UX surfaces themselves unchanged since the single-path round: wireframes **v12** (`ec834ea8d`, 157 wrappers incl. the X-01 exhibit), demo + deck republished, verifier families F1–F9 resolved.

## The surfaces (where everything lives)

| Surface | Location | Sync rule |
|---|---|---|
| **Spec (source of truth)** | `social-recovery-ux-flows.md` — flows A–G, decisions log **1–98**, display rules, persistence table, **Milestones section (MVP/V1/V2)**, tech-deps | Everything else follows it |
| **Wireframes** | `social-wireframes.pen` — **v15, 165 `Page ·` wrappers = 137 active + 28 in `Flow · OUTDATED`** (reconciliation round, decisions 90–98); band `Flow · X` holds the team exhibits X-01 (policy models) and X-02 (rollout milestones) | Edited ONLY by the Pen design agent via prompts; the driving agent writes prompts, verifies via git-JSON, commits after the user's ⌘S |
| **Notion replica** | [Social Recovery UX Draft](https://app.notion.com/p/3ba9a4c092c780a492f2d144c148762b) | RETIRED (2026-08-25): reviews moved to the GitHub PR — do not sync |
| **Review deck** (teammates) | artifact `ca87aff0-…65e3`, source `social-recovery-ux-assets/recovery-ux-review.html` | Edit the repo file, republish with `url` param |
| **Persona demo** ("Recovery Journeys") | artifact `b822a105-…08b0f`, sources in `social-recovery-ux-assets/persona-demo/` (journeys.json + template + build-demo.py) | Re-export screens from the .pen, run `build-demo.py`, republish with `url` |
| **Team review comments** | [Persona demo review](https://app.notion.com/p/3bf9a4c092c78193aa1fdf53a705e8ed) — per-persona sections, citation format `Persona · N/M · screen-code` | Triage comments into approval rounds when they arrive |
| **Kit-team asks** | `social-recovery-kit-asks.md` (source, **v2**) + Notion page [Asks for the kit team](https://app.notion.com/p/3bf9a4c092c7818a827dce1146c7747a) (stale, pre-dates the file) | Repo file is authoritative; update when a blocker resolves; sync Notion on request |
| **SDK requirements** | `social-recovery-sdk-requirements.md` (source, **v9**) + Notion replica [SDK requirements — Account recovery](https://app.notion.com/p/3c29a4c092c7812b8b4bdbb6ae3001ec) (**frozen at v7 — Notion sync retired 2026-08-25, reviews on the GitHub PR**) | Edit the repo file, then sync Notion with targeted `update_content` ops — keep them targeted so future inline discussions survive |
| **Extension work items** | `social-recovery-extension-work.md` — MVP/V1/V2 tagged | Repo only; companion to the SDK doc |
| **zkPassport research** | `social-recovery-zkpassport-research.md` — synthesis + raw fronts A/B/C as appendices | Repo only; re-run the fronts if we revisit decision 80 |

## What was built (chronology, compressed)

1. **The spec** — flow diagrams for setup (C: presets landing + guided wizard + advanced builder), recovery (D, chaptered), owner cancel (D2), guardian page (E1 sync default, E2 future), health checks (F, optional), management (G). Built by Q&A, hardened by a 32-item doc review and a PR review.
2. **The model decisions** — generic ANY-of groups (41): a path = required rows + M-of-N groups over mixed members; guardians are ordinary members; per-path waiting periods (48h default, 12).
3. **Wireframes v1→v10** — 128 lo-fi screens in flow bands, one review round of 82 findings, persona-driven verification rounds, and the attack round. Conventions: `Page · <code>` wrappers, Component / Threshold, red design-note tokens, PROVISIONAL dashed treatment, no guardian names anywhere (43).
4. **The persona demo** — click-through walkthrough over real exports: 5 personas (Sam/Alice/Carl/Diana/Bob) × Setup + Recovery acts (Bob adds Attack), an Extras card (provisional + all-methods dry-run), verbatim persona docs, step deep links (`#persona/step`), act tabs.
5. **The meeting round** (decisions 47–57) — dry run before save, recovery-password step (no hint, set-once), single-method copy, synced-passkey cleanup semantics, guardian page relative-path-first, `.eml`-local ZK Email direction (55, retires the code illustration), "extension password" naming.
6. **The attack round** (2026-08-18) — six Opus 5 fronts (ease, copy, missing states, consistency, collisions, implementability) → 89 findings → 55 approved → decisions 58–72, two Pen sweeps (92 frames edited, 20 screens added).
7. **The single-path model change** (2026-08-18, decisions 73–78) — team decision: ONE recovery path per account (no OR-of-paths). Path = required rows AND ANY-of groups; wizard builds at most one group, multiple groups are Advanced-only; instance uniqueness; one waiting period, 24h contract floor (48h UI default); ZK Email becomes receive-and-upload via an external send-only relayer + a "How to get the .eml" help sheet (Gmail/Outlook web/Apple Mail/Yahoo/Proton); 4337 focus (7702 deferred, record-only). Persona remap approved: Sam = passkey only · Alice = [3/5] guardians · Carl = passkey AND Sara (both required) · Diana = [1/2](passkey, zk email) · Bob = [2/3](passkey, guardian, zk email) via Advanced.

## Round in flight (single-path model)

Done: spec (`4b1232964`) · Notion sync · Pen round, verified (git-JSON: 28 moves, 25 rebuilds + C-05n, 95 content edits + property-only sweeps confirmed by full-JSON hash) · committed as v11 (`59944b2fa`) · step-label micro-fix verified (PASS: exactly the 9 frames changed, closure test reproduces the baseline hash; C-09/C-09c "OF 9" strings are pre-existing single-account annotations, fine) · committed as v11.1 (`89142332d`) · journeys v8: remapped to the one-path personas (23 screen renames, 22 story/spec rewrites, Diana bio, C-05n in Extras; 72 fresh exports + 16 fresh hotspots — C-07g's button is "Save recovery path", C-10b's is "Add", the rebuilt C-10c has no advance button so Bob's step has no hotspot) · **demo REPUBLISHED** (`b822a105…`, label single-path-model) · **deck REPUBLISHED** (`ca87aff0…`; targeted single-path edits by an Opus 5 subagent, zero stale phrases; note: deck's "eight steps" wizard heading is pre-existing drift vs decision 58's ten, left alone). Still open in this round:
1. **Verifier triage.** Five Opus 5 per-persona verifiers ran on the rebuilt demo: 66 findings (24 blockers). Clean across all five: zero multi-path UI remnants, all wizard labels OF 10, receive-and-upload correct everywhere. Merged families awaiting Fibo's per-family approval: F1 Bob's rebuilt builder trio (C-10/C-10c/G-01) draws required-passkey + guardian group instead of his lone [2/3](passkey,email,guardian) — Pen fix. F2 generic recovery readouts (D-04/D-07*/D-11/D-13/D-15) draw passkey+2-of-3 and contradict Sam/Diana/Carl — bridges vs persona variants, Fibo decides. F3 journeys play review (step 8) before dry run (step 7) — journeys reorder. F4 C-05 side panel lists methods the persona never picked — bridge notes. F5 stale story text/labels (~14 items) — journeys fixes. F6 dead/misplaced hotspots (7) — bounds re-pass. F7 screen defects for a Pen micro-round: C-07f visible-vs-hidden panel, C-07m progress mismatch, D-08 count-bearing footer + failure-only state, D-15 email mask mismatch, D-07f/D-07i decline-state inconsistency, D-02b same-install list on a fresh install. F8 design tension: preset "device+email" is AND-required while the wizard recommends [1 of 2] for the same inventory — Fibo to rule. F9 accepted/no action: "relayer" inside red design notes, alex.eth fixture, address drift between frames, mid-ceremony device switch.

**RESOLVED (Fibo's rulings, all landed in v12 + journeys v9):** F1 Pen redraw (Bob trio = lone [2/3] group) · F2 hybrid (new D-04d + D-15d for Sam and Carl; bridge notes elsewhere) · F3 reorder (dry run step 7 before review step 8, all four wizard journeys) · F4 bridges · F5/F6 journeys fixes (verifier-measured rects; Carl's D-08 step now narrates the reject-then-retry beat by design) · F7 six Pen pixel fixes · F8 keep the AND preset + "Deliberately strict" line on C-01 · F9 accepted. Plus Fibo's new **X-01 exhibit** (three policy grammars compared; Extras act, no persona mapping). Both rounds verified PASS by Opus 5 subagents; demo republished (75 screens, label verifier-fixes-v12).

**Clipping baseline UPDATE:** the accepted scan baseline is now **1 + 4**: D2-01 "Action · Receive" plus four decorative dash-rule overflows in C-07k and D-07e (last dash of a dashed rule inside a clip strip — by design). Disabled (`enabled:false`) multi-path leftovers also appear in raw scans; ignore them.

### Pen round — what landed (2026-08-18)

**Part 1 — the OUTDATED band.** New root band `Flow · OUTDATED · MULTI-PATH MODEL (superseded 2026-08-18)` at `x=0, y=13546` (Flow G's bottom edge + a 260px gutter), same anatomy as the other bands. The 28 invalidated `Page ·` wrappers moved into its Screens row in the given order: C-01, C-04, C-04b, C-04e, C-05e, C-05k, C-06b, C-07, C-07g, C-07k, C-10, C-10b, C-10c, C-10d, C-10e, C-10f, D-04, D-06, D-06b, D-06e, D-06f, D-07d, D-07e, G-01, G-01b, G-01c, G-02b, G-03.

> **Convention change vs. the earlier plan:** the moved frames were **not** renamed to `<code>-old` (decision 67 — never rename existing frames). Instead each moved wrapper's caption **description** is prefixed `OUTDATED · `. Frame names and codes are untouched, so old review notes still resolve.

C-06b (dominated-path warning), D-07d (switch-path confirm) and G-02b (simple remove confirm) got **no replacement** — those concepts died with the model.

**Part 2 — 25 rebuilds + 1 new screen**, each in a fresh `Page · <code>` wrapper sorted back into its original band's Screens row by code. New short-names keep the code prefix:

| Code | New frame short-name |
|---|---|
| C-01 | `C-01-choose-your-recovery-path-presets` |
| C-04 | `C-04-recommended-path-single` |
| C-04b | `C-04b-recommended-path-either-one` |
| C-04e | `C-04e-adjust-your-path-single` |
| C-05e | `C-05e-verify-your-email-receive-upload` |
| C-05k | `C-05k-email-check-failed-eml` |
| **C-05n (new)** | `C-05n-how-to-get-the-eml-sheet` |
| C-07 | `C-07-review-and-confirm-single` |
| C-07g | `C-07g-review-either-one-works` |
| C-07k | `C-07k-dry-run-either-one-works` |
| C-10 | `C-10-advanced-builder-single-path` |
| C-10b | `C-10b-add-member-instance-uniqueness` |
| C-10c | `C-10c-advanced-builder-saved-single` |
| C-10d | `C-10d-add-member-optional-test` |
| C-10e | `C-10e-add-method-to-library-single` |
| C-10f | `C-10f-advanced-builder-first-run` |
| D-04 | `D-04-confirm-identity-single-path` |
| D-06 | `D-06-your-recovery-path-readout` |
| D-06b | `D-06b-path-one-member-unavailable` |
| D-06e | `D-06e-wrong-recovery-password-single` |
| D-06f | `D-06f-your-recovery-path-values-visible` |
| D-07e | `D-07e-verify-with-your-email-eml` |
| G-01 | `G-01-management-overview-single-path` |
| G-01b | `G-01b-cleanup-deferred-single` |
| G-01c | `G-01c-triage-deferred-single` |
| G-03 | `G-03-member-still-needed-in-place` |

C-05n is a plain full-screen help sheet (five client rows: Gmail, Outlook web, Apple Mail, Yahoo, Proton) placed after C-05l; it is opened from both C-05e and D-07e.

**Part 3 — the copy sweep.** ~125 text nodes edited in place across bands A, B, C, D, D2, F and G (E1 untouched, as specified). Every plural/multi-path implication is gone; the residual regex hits are all the intended new copy ("there is no other path"). Structural plural artefacts that copy alone could not remove were **disabled** (`enabled: false`) rather than deleted, so they are one flag away from returning: the second path card + OR divider + "Add a recovery path" button on G-02, G-03b, G-04 and D2-03; the second per-path waiting-period card on C-06; the "Add another recovery path" and "Customize instead" buttons on C-04c; the "Your other recovery path" row on G-05b; and the **11 "Switch path exit" footers** across the D-07 family and D-08.

Notable non-mechanical parts of the sweep:
- **C-06 / C-06c**: collapsed to ONE account-level waiting period, chips are now 24h / 48h / 72h / 7 days (the old `Custom…` chip became `7 days`), a "The minimum waiting period is 24 hours." line sits under the chips, and a red note records the on-chain floor (decision 74).
- **C-07b**: base content **rebuilt** to the new C-07 review anatomy, keeping the save-failure card above the review blocks (decision 63).
- **D-05b**: retitled to "Path can't be satisfied" — the copy is now "You can't complete this recovery path right now. There is no other path." plus a WHAT TO TRY panel (contact the guardians, find the device). Dead-end anatomy kept.
- **G-02**: action is "Remove recovery"; the type-to-confirm modal reads "This removes your recovery path. The account becomes UNPROTECTED."
- **G-05**: titled "Edit your recovery path" and carries a new note that the member picker disables duplicates (decision 76).

**Clipping baseline holds:** the scan reports exactly one enabled issue, D2-01 "Action · Receive" (accepted).

### Step-label micro-fix — APPLIED

Decision 58 stands: the wizard map is **ten steps**, and nothing in the single-path model drops one ("OF 9" exists only for single-account wallets that skip step 10). The 9 rebuilt frames that briefly read `OF 9` were corrected in place — label text plus a ten-dot indicator with the right dot active:

- C-04, C-04b, C-04e → `STEP 3 OF 10`
- C-05e, C-05k → `STEP 4 OF 10`
- C-07k (dry run) → `STEP 7 OF 10`
- C-07, C-07b, C-07g (review) → `STEP 8 OF 10`

Verified: **no `OF 9` label remains** anywhere in the document, all 46 active wizard indicators carry ten dots, and the clipping baseline still reports exactly one enabled issue (D2-01 "Action · Receive").

### Pen round — fifth review sweep (2026-08-27, decisions 99–102 + defects 31–35) — APPLIED; SAVED EXCEPT THE MICRO-ROUND BELOW

Scope respected: `Flow · OUTDATED` untouched, `Flow · X` touched only in the two named strings, no zkPassport/ZK Email screen edited (C-05e, C-05k, C-05n, D-07e).

**A · Waiting-period floor (decision 100).** C-06: both `Instant (0h)` chips deleted (enabled path + the disabled `device + email` copy) and the Instant warning line removed; `24h` is now the first chip. C-06c: same. Design note on both → "24h wallet minimum — decision 100 (the chain enforces none)". C-07/C-07b/C-07g hint → "Minimum 24 hours (wallet rule — the chain enforces none). Default 48 hours." C-10/C-10b/C-10c/C-10d/C-10f Min line → "Minimum 24 hours (wallet rule) — default 48 hours." X-01 and X-02 strings updated to "24h wallet minimum, 48h default".

**B · Privacy levels renamed, Private is default (decision 99 + defect 34).** C-06e: the three options are now **Private / Shape visible / Public**; the selected+accent treatment and the `DEFAULT` chip moved from the middle option to Private (both option frames rebuilt via `Replace`, so their ids changed: Private `wz6tY`, Shape visible `flxiA`). Private's reveal line gained the "Anyone can still see that this account has a recovery setup." clause. Lead → "Required unless you choose Public. This does not change who can recover your account — only what a stranger can read about it." The `[Foot]` line was deleted whole (it ended in the now-false "forgetting it never stops a recovery" clause; the honesty line below still carries the loss consequence). D-06f is framed as the default state: caption → "Your recovery path — locked (Private — default)", lead says the owner chose Private, the side note now says “Private” and keeps "the chain shows a recovery module and nothing else". D-06 is framed as the Shape-visible state: structure note first sentence replaced with "The shape is readable because this setup chose “Shape visible”. At Private, nothing shows before the password." (second sentence about masked values kept), and its caption gained "— shape visible" for parity with D-06f.

**C · Recovery Card.** C-08 + C-08b: the `Recovery page` label + URL rows and the OPEN QUESTION hosting panels are deleted (decision 51 settled it); how-to step 1 → "1.  Install Kohaku on any device and choose “Recover an account”."; the "This card alone cannot move funds." footer line was already present on both. G-01 + G-02 card `[Sub]` → "…carries this account's address and recovery password, and the browser alert…" (decision 90).

**D · D-08 paste screen.** The "An approval has no time limit" note is deleted (contradicts decision 91). A fifth error variant was added between "You already added this approval" and the Collected row: "This approval has expired" / "Approvals carry a validity window. Ask this member to sign again." Checks note → "…the wallet checks four things: … and the approval has not expired." The screen frame grew 975 → 1004 so the Actions row stays inside.

**E · Graded meter conversion (decision 86, defect 33).** G-02, G-03b, G-04 and D2-03 lost the `Protection meter` (label row + bars) and now carry G-01's plain status row: icon + `[Status label]` + hint. D2-03 keeps its own terminal wording ("Control moved away" / "Your recovery path no longer protects this key") with a `shield-off` icon. The four "A single-method setup is never plain “Recovery ready”" design notes — including the `· one method` qualifier specimens — were replaced in place by G-01's "Graded meter + fragility report moved to V2 / Decision 86." note. F-01/F-02 keep their meter (V2 screens).

**F · D2-01 rollback (decision 101).** The whole second-state card (`SAME BANNER · SECOND STATE · WAITING PERIOD ENDED`, the ended chip, the state note, the ended caption, the "try to cancel" button) is deleted; the primary label reads "It wasn't me — cancel it" again; the also-cancels line and the V1 counter-proof block stay. Screen height 1000 → 800.

**Clipping scan: baseline holds exactly.** Full-depth scan reports 5 enabled issues — D2-01 "Action · Receive" plus the four decorative dash-rule overflows in C-07k (D29 ×2) and D-07e (D4 ×2) — and 15 `enabled:false` multi-path leftovers to ignore. Note for future scans: a `depth`-capped `Get` visitor misses the four dash-rule items, so run the scan with no depth option.

**Left deliberately out of scope (flag for the next round).** (a) The same two contradictions fixed on D-08 still live on other screens the prompt did not name: "An approval has no time limit" notes on D-07f, D-07g, D-07i, D-07j, D-07k, the "checks three things" note on D-07f/D-07g/D-07k, and E1-06's "There is no time limit." (b) Live G-02 still draws the pre-decision-73 multi-path main column (two paths, OR divider, "Add a recovery path"). (c) After the D-04/D-04d path block deletion, D-04's "What you'll need" panel is down to a single item ("Nothing is signed on this step."); its design note was retitled "The readout owns the path (decision 102)" on both screens.

**Micro-round (same day, after Fibo's review of the saved file).** One defect and the decision-91 leftovers, applied in the editor and still awaiting ⌘S:

- **D-08** — the new `Variant · This approval has expired` frame had kept the copied text. Cause: a `Copy` call with `descendants` keyed by descendant NAMES ("Title", "Body") silently applied nothing. Fixed with plain `Update` calls on the copy's own text ids (`YnnzJ`, `hTuvH`). Lesson: after `Copy`, read the copy back and override by id — do not trust name-keyed `descendants`.
- **D-07f, D-07g, D-07i, D-07j, D-07k** — the `Note · An approval has no time limit` side note is deleted on all five (frame, title and text).
- **D-07f, D-07g, D-07k** — paste-checks line now names four checks, matching D-08.
- **E1-06** — "There is no time limit…" → "Mind the expiry on the request — an approval is only valid until its “valid until” moment. Open this link again once you have checked with the owner."

Clipping scan after the micro-round: baseline holds, 5 enabled issues (D2-01 plus the four dash-rule items) and 15 disabled leftovers.

### Pen round — G-02 single-path rebuild + D-04 retitle (2026-08-27, after v16) — APPLIED IN EDITOR, AWAITING ⌘S

**G-02 body rebuilt to decision 73.** Deleted: both path cards (`Path · Your device + your people`, `Path · Your device + your email`, each carrying its own `Remove path button`), the `OR divider`, and the `Add path button`. Built in their place one read-only `Path card · your recovery path` (`eTSlb`) that mirrors G-05's fixture and shape: head "Your device + your guardians" + `Live` chip + `Edit` link, then `REQUIRED` Passkey — this device, `AND`, a `Component / Threshold` ref reading "any 2 of 3 members", the three guardian addresses masked as `0x8a5D…dc41 · 0x3c7B…fa9d · 0x77E4…c1e2`, and a foot "Waiting period 48h". Under it: the consequences line "Removing recovery uninstalls the module. Your key keeps working — but if you lose it afterwards, nothing can bring the account back.", then ONE destructive action, `Remove recovery` (outline + `trash-2`, matching G-01's button; accent fill stays reserved for positive primaries). The v16 "Recovery is set up" status row and the Recovery Card & alerts row are untouched. No ZK Email string remains anywhere on the screen.

**The modal was KEPT, not deleted.** The prompt listed `Last path warning modal` for deletion but the next bullet requires "the existing remove-confirm modal" to stay with its current copy — and that node IS the only confirm modal on the screen (G-02's caption is "Remove recovery warning"). So the multi-path FRAMING went and the modal stayed: renamed `Remove recovery modal`, copy untouched, including "Removing recovery cancels the recovery currently in progress. Uninstalling the module does the same." and the type-REMOVE field. Same reasoning for two names: the screen frame is now `G-02-remove-recovery-warning` (was `…-remove-last-path-warning`). **If the intent really was to drop the modal, say so and it goes in one call.**

**Screen height 955 → 800.** The shorter body allowed it, and it fixes a pre-existing mismatch: the `Modal overlay` is a fixed 1280×800, so on the 955-tall frame the bottom 155px of the screen sat outside the dimmed layer.

**D-04 + D-04d — panel retitled** "What you'll need" → "Good to know" (frame names too). D-04 holds the single line "Nothing is signed on this step."; D-04d carries two lines ("One method answers this path — your passkey." + the same one), and both read as good-to-know facts, so it was retitled as well.

**Clipping scan:** baseline holds — 5 enabled issues (D2-01 "Action · Receive" + the four dash-rule items in C-07k and D-07e), 15 `enabled:false` leftovers.

## Key decisions (themes; the log rows 1–78 in the spec are authoritative)

- **Vocabulary:** Account recovery / recovery path / method / waiting period (5); forbidden in UI: policy, proof, relayer, atomic (5, 24-sweep); guardian = role, member = group slot, approval = the artefact (61); "extension password" vs the separate "recovery password" (57, 48); presets: "Your device + your guardians", "Guardians only" (61).
- **Structure:** ten-step wizard map (58); group counts as one unit (60); threshold component everywhere read-only (62); error states preserve base (63); Back on every step (64); chip vocabulary (59).
- **Security honesty:** honest floor copy (6), no fake expiry on approvals (9), cancel-is-not-resolution triage (D2), no guardian names (43), card is a non-map (contents decided; hint dropped), per-request dismissal (66).
- **Recovery password:** skippable step 6; set once, no change surface; wrong password never blocks recovery (48/56); what gets encrypted is open (53/Q29).
- **ZK Email:** receive-and-upload (77) — the user asks for the email, receives it at the enrolled address, downloads the original `.eml` and uploads it; the proof is computed locally (55). The word "relayer" never appears in UI copy. Code illustration retired; subject format / accountCode / prover stack open (Q16).

## Pending — waiting on other people

1. **Team review comments** on the demo (Notion page above). None triaged yet.
2. **Ace 0x — Q16 remainder:** subject/command format; where the accountCode lives without a backend (sharpest); prover stack + ~15–60s in-extension latency; DKIM registry choice; module timelock/expiry vs our waiting period.
3. **Kit team — four asks** (see the asks page): funding/executor (Q7/19, blocker), group encoding (T10), value visibility (Q29), encryption scope (53). Plus the new **concurrency** question (two pending recoveries?, tech-deps row M10).
4. **Nico** — Advanced builder layout input; C-10 work frozen meanwhile.
5. **Contracts team** (`defi-wonderland/mast-social-recovery` PR #3, branch `design/spec-v1`) — the reconciliation report is `social-recovery-contracts-spec-review.md` — open items only, stable REC-nn ids (history in git + the appendix). **Nothing sent to them yet.** Outbound queue (report §3, REC-15…24): current docs, the wait default (they want days, we say 48h), Q-12 disclosure copy, the Q-8 answer (rendering ours + trust-root declarations as data + destination-key copy), the `slot` question, the stale Ambire premise (Q-19), decision 51 as settled.

## Pending — our side, deliberately deferred

- **Attack-round deferrals** (also listed in the spec's What-persists section): M9 stale-local-state family (re-read on resume, draft-out-of-date states), M13 attack-during-triage band, M16 guardian changed-mind block on E1-05, M23 alerts-honesty rewrite (C-08/D-13), M34 DKIM-rotation failure split.
- **Milestone split — DONE as decision 79** (2026-08-19, spec "Milestones" section). Remaining follow-ups: align the split with Ace 0x; run the queued Pen items (D-07 manual-sign state, MVP terminal honesty copy) when Fibo asks.
- **Provisional screens' fate** (C-05i optional guardian test, C-06d recheck strip) — keep or remove after the team reviews the demo's Extras card.
- **E1-04 mobile variant** — accepted as missing for now.
- **Demo extras** offered, not requested: Daniel/Aadhaar journey, health-check maintenance act.

## Decisions still to make (blocked or unowned)

- Funding/executor model (Q7/19) — blocks final Flow D copy and D-13b's real shape.
- Encryption scope (53) + value visibility (Q29) — blocks final password-step copy, D-06 family predicate, accountCode storage.
- Concurrency rule (one pending recovery per account?) — blocks the pending-band copy.
- Q16 remainder — blocks the final C-05e/D-07e polish.
- Whether the provisional screens ship (see above).

## Working agreements (how to continue without breaking things)

- **Loop:** driving agent writes Pen prompts → user runs the Pen agent → user presses ⌘S (verify the file actually changed — a save can predate the edits) → verify via git-JSON per-screen signatures (`git show <rev>:social-wireframes.pen`, never app exports of copies) → commit → sync spec/Notion/deck/demo as needed.
- **Reviews:** multi-agent fronts → merged dedup list → per-item user approval → split into doc round + Pen prompts. Nothing applies without approval.
- **Known baseline:** the clipping scan must report exactly ONE issue (D2-01 "Action · Receive", accepted). Anything else is a regression.
- **Scratchpads get wiped**; every artifact source lives in the repo. If something is lost anyway, replay the Write/Edit history from the session transcripts in `~/.claude/projects/-Users-fiboape-repos-internal-kohaku-kohaku-extension/`.

## Doc version histories (moved out of the doc headers, 2026-08-27 — decision at PR thread :3)

**`social-recovery-ux-flows.md`** — reviewed: 6-perspective review pass applied 2026-08-12; decisions 1–98 current as of 2026-08-25, including the contracts-spec reconciliation round (decisions 90–96, REC items) and the final ruling batch (decisions 97–98: privacy levels assumed available, setup disclosures adopted); decisions 99–102 added 2026-08-27 (fifth review round).

**`social-recovery-sdk-requirements.md`** — v9 (2026-08-25): new assumption A7 (all three privacy levels assumed available incl. the middle shape their model lacks — kit-ask REC-24), decisions 97/98 recorded, REC-25 resolved (two-state getRecoveryConfig), disclosure rendering stays UX-side (REC-18/A6). v8: reconciliation round (REC-7/9/10/11/12/13/20/25) — eleven-field guardian digest, PendingRecovery loses satisfiedMethodIds gains attemptId, PreparedTx third-party-call kind, Claim.expiresAt, §2.6 drops the "wrong password never blocks recovery" promise, §2.7 SDK duties. v7: amendments 88 (fast-track creates only an EOA signer) + 84 (public = no password, encryptConfigValues skipped). v6: fourth PR review round, decisions 84–89 (commit-reveal answered, floor removed, grading → V2, user-funded MVP + gas screens, smart-account-at-init, E2 skipped). v10 (2026-08-27): fifth review round — see the checkpoint above.

**`social-recovery-extension-work.md`** — v5 (2026-08-25): reconciliation round (REC-20 adapter = kit deliverable, REC-41 no kit watcher). v4 (2026-08-24): fourth review round (decisions 84–89); substrate = bare-bones 4337, not Ambire (the Ambire-specific blockers — privilege commitments, CALL_TO_SELF rejection, recovery outside validateUserOp — no longer apply); methods per decision 80: passkey, guardians, Anon Aadhaar, zkPassport. v6 (2026-08-27): fifth review round — see the checkpoint above.
